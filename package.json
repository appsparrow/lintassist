{
  "name": "lintassist",
  "private": true,
  "description": "Static site — source lives in public/, copied to dist/ for Cloudflare Pages (project's configured output directory).",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/"
  }
}
